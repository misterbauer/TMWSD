<% provide(:title, @user.name) %>
	<div class="row user-profile">
	<aside class="col-md-3 col-md-offset-2">
		<section id="user-info" class="user_info">
			<%= gravatar_for @user %>
			<h1><%= @user.name %></h1>
			<h2><%= @user.email %></h2>
		</section>
	</aside>	
	<div class="col-md-5 user-messages">
	<% if @user.messages.any? %>
		<table style="width:100%">
		  <tr>
		    <th>URL</th>
		    <th>STATUS</th>
		  </tr>
		  <%= render @user.messages %>
		</table>
	<% else %>
		<h2 class="center">No Messages</h2>
	<% end %>
	</div>

</div>